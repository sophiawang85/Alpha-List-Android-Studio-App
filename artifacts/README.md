# Original build artifacts

`app-debug.apk` and `output-metadata.json` are preserved from the original submission. They are not regenerated or verified by the repository reorganization. New builds are written to `app/build/outputs/`.
